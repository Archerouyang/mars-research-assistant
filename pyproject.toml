[project]
name = "mars-skills"
version = "1.0.1"
description = "Development tooling for the independent Mars Skills collection."
license = "MIT"
requires-python = ">=3.12,<3.13"
dependencies = [
    "yfinance==1.5.2",
]

[tool.uv]
package = false
