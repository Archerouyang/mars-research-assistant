# 1.0 Acceptance Results

Date: 2026-07-08

This records the first fresh-chat acceptance pass for the local Trading
Research System workflow. It is a workflow validation note, not a public release
tag or semantic-version claim.

No private broker exports, raw account records, private runtime files, or
paywalled research text are copied into this repository.

## Baseline

| Item | Result |
| --- | --- |
| Branch baseline | `dev` at `89e8156` |
| Installed plugin cache | `trading-research-system@personal` with cachebuster `20260708134208` |
| Runtime path | `/Users/archer/Documents/dailytrades-runtime` |
| Repo state before prompts | clean |
| `bash scripts/verify-plugin.sh` | pass |
| `bash scripts/verify-mvp.sh` | pass |
| `git diff --check` | pass |

Runtime health at the start of this acceptance pass:

- Available: `market-plan.md`, `trading-profile.md`, `updates/`,
  `daily/2026-07-08/`, and a standard `portfolio_snapshot.csv`.
- Missing or not ready: `macro-panel.json`, `ops-state.md`,
  `trade-plans.csv`, `intraday-watchlist.csv`, and KVN store.
- Broker and macro live sources: Longbridge broker skill, Longbridge Terminal
  CLI, Longbridge macrodata, and IBKR connector were not available as authorized
  live reads in this validation context.

## Prompt Results

| # | Prompt | Result | Evidence |
| --- | --- | --- | --- |
| 1 | `开始今天的交易研究` | PASS | Fresh chat reported formal runtime health, broker-source health, current mode, macro-data status, missing confirmations, and a Chinese next-step guide without writing runtime or repo files. |
| 2 | `做本周交易计划 / 下周展望` | PASS | Fresh chat read Active Market Plan and update trail, disclosed missing `macro-panel.json`, used source routing, explained macro/rates/policy/news impact, mapped current-holding and setup-pool impact, and explained internal state labels in Chinese. |
| 3 | `更新今天盘前/盘中计划，检查计划内 setup` | PARTIAL | Fresh chat read plan/update trail and skill references, then correctly reported that formal scanner execution was blocked by missing `intraday-watchlist.csv`, `trade-plans.csv`, `macro-panel.json`, and unauthorized live data. Repo fixture contracts passed, but today's runtime package could only mirror setup state. |
| 4 | `看一下我的持仓日报/组合风险` | PASS | Fresh chat used the standard runtime `portfolio_snapshot.csv` view and rendered a position-daily-report style summary with concentration, instrument/product exposure, broker/account coverage, cash, leveraged/single-stock ETF path risk, and decisions needed today. Raw private broker exports were not copied. |
| 5 | `做 DRAM/SOXX/QQQ 的滚动盘面分析` | PASS | Fresh-chat rerun `019f4613-1145-7783-bc34-73f9f7b276b4` used installed plugin `0.1.0+codex.20260709083506`, read saved Longbridge standard OHLCV for DRAM/SOXX/QQQ, found prior runtime context, labelled 1D/1H timeframes, produced timeframe-owned support/resistance and proportional add/trim/pause zones, mapped weekly events, and did not read broker accounts or write repo/runtime files. |
| 6 | `我刚下单了 / 这笔结束了，帮我复盘` | PASS | Fresh chat routed to the two-stage review flow and separated post-order context capture from post-exit result review, with objective broker facts gated behind authorization or user-provided facts. |

Summary: 5 PASS, 1 PARTIAL, 0 FAIL.

## Follow-up: 2026-07-09

After the first acceptance pass, `dev` added and ran a safe daily runtime package
preparation flow for `2026-07-09`. The private runtime now has today's
`daily/2026-07-09/`, `trade-plans.csv`, `intraday-watchlist.csv`,
`portfolio_snapshot.csv`, `ops-state.md`, and update note containers. These
files are header-only or private runtime files and are not committed to this
repo.

The empty/header-only intraday scan path now returns a safe operational result:
it reports `没有已准备的 setup 行` and says it cannot claim `approaching` or
`triggered` signals until the watchlist is populated. This fixes the
runtime-file-missing blocker for prompt 3, but does not by itself produce
prepared setup rows.

Fresh-chat prompt 3 was rerun in thread
`019f44c0-3f0a-7660-9929-a7a44388b12f` against the installed personal plugin.
Result stayed `PARTIAL`: runtime health, skill/reference reading, and
`intraday_scan.py` behavior were correct, but today's plan files are
header-only, `macro-panel.json` is still missing, Longbridge/IBKR were not
authorized inside that fresh chat, and update trail content was empty. The rerun
also exposed an installed-package verifier issue: contract verifiers running
from the personal cache resolved repo docs to the cache parent. `dev` now fixes
`PluginPaths.from_script` so installed verifiers can use the project checkout
from cwd for repo docs while keeping plugin files rooted in the installed cache.

IBKR read-only forward smoke was also performed in this development thread:
authorized read-only positions, balances, and allocation calls returned usable
data; `ibkr_connector_adapter.py` now maps saved IBKR connector JSON into the
standard `portfolio_snapshot.csv`; and `position_daily_report.py` rendered a
private temporary report from that standard view. The raw temporary JSON and
snapshot were deleted from `/private/tmp`, and no private broker data was
committed.

Validation after the follow-up:

- Commit: `f692d5e Add IBKR connector snapshot adapter`
- Installed plugin: `trading-research-system@personal`
  `0.1.0+codex.20260709050101`
- Passed: `verify_daily_runtime_package_selftest.py`
- Passed: `verify_daily_runtime_package_contract.py`
- Passed: `scripts/verify-plugin.sh`
- Passed: `scripts/verify-mvp.sh`
- Passed: `python3 -m compileall plugins/trading-research-system/scripts`
- Passed: `git diff --check`
- Passed: `verify_ibkr_connector_adapter_selftest.py`
- Passed: `verify_ibkr_connector_adapter_contract.py`
- Passed from installed personal plugin cache:
  `verify_1_0_acceptance_contract.py`
- Passed from installed personal plugin cache:
  `verify_ibkr_connector_adapter_contract.py`

Macro-panel runtime preparation follow-up:

- Commit: `7fcbac6 Add macro panel runtime preparation`
- Installed plugin: `trading-research-system@personal`
  `0.1.0+codex.20260709052038`
- Passed: `verify_prepare_macro_panel_selftest.py`
- Passed: `verify_prepare_macro_panel_contract.py`
- Passed from installed personal plugin cache:
  `verify_prepare_macro_panel_contract.py`
- Follow-up source-label fix: official fallback JSON must use
  `--source-capability official_source_fallback` and preserve item-level
  `source`, so fallback macro panels are not mislabeled as Longbridge
  macrodata.
- Official fallback forward smoke: `prepare_macro_panel.py` generated today's
  private runtime `daily/2026-07-09/macro-panel.json` from Treasury and FRED
  public/official fallback data. Runtime health now reports `macro_panel`
  available and `official_source_fallback=available`. Gold is now treated as an
  optional confirmation input rather than a required posture input. Today's
  private panel was regenerated with `degraded=false`,
  `strategy_posture=defensive`, `missing_required_indicators=[]`, and
  `missing_optional_indicators=["Gold"]`. Longbridge live macrodata remains
  unverified in this chat.

Fresh-chat #1/#3 combined rerun after the macro-panel fix:

- Thread: `019f458c-7172-79c1-ae89-080674a39bd1`
- Installed plugin: `trading-research-system@personal`
  `0.1.0+codex.20260709062046`
- Result: `PASS` for behavior acceptance.
- Evidence: the fresh chat loaded the new installed cache, reported
  `macro-panel.json` as available, `source_capability=official_source_fallback`,
  `degraded=false`, `strategy_posture=defensive`,
  `missing_required_indicators=[]`, and
  `missing_optional_indicators=["Gold"]`.
- It also confirmed `trade-plans.csv` and `intraday-watchlist.csv` are
  header-only and ran `intraday_scan.py` in a read-only path. The scanner
  correctly returned no prepared setup rows and did not claim any setup was
  `approaching` or `triggered`.
- Scope note: this is a workflow behavior PASS. It does not mean a real
  intraday setup state was produced; prepared setup rows still need to be
  populated before formal setup monitoring can produce trading-relevant states.

Setup row preparation follow-up:

- Added `prepare_setup_rows.py`, which consumes user-confirmed setup JSON and
  writes scanner-ready `trade-plans.csv` and `intraday-watchlist.csv`.
- Fixture verification proved the bridge from confirmed setup JSON to
  `intraday_scan.py`: a temporary runtime produced real `triggered` and
  `needs_review` setup states without broker reads, market data, or order
  actions.
- Private 2026-07-09 runtime dry-run showed both daily CSVs are header-only and
  would be filled by `prepare_setup_rows.py` after user confirmation.
- Current Active Market Plan setup pool is explicitly watch-only / `needs_review`
  with no new exposure until reopened, so the plugin should not silently promote
  those rows into active trade plans without user confirmation.

PA OHLCV rollforward follow-up:

- Added `price_action_rollforward.py`, which consumes authorized or
  user-provided OHLCV JSON and produces a consistent rolling PA note with
  explicit main/aux timeframes, prior-analysis comparison, support/resistance,
  proportional add/trim/pause zones, cost context, and event mapping.
- Fixture verification proved the script path can turn local OHLCV fixture data
  into a DRAM/SOXX-style note without broker reads, market-data fetching, or
  order actions.
- Added Longbridge OHLCV adapter support via `longbridge_ohlcv_adapter.py`,
  which converts saved Longbridge kline JSON into the standard OHLCV JSON
  consumed by `price_action_rollforward.py`.
- Longbridge OHLCV forward smoke passed on 2026-07-09: QQQ.US and SOXX.US
  each produced 90 daily candles, DRAM.US produced 66 daily candles, and all
  three generated private PA rollforward notes under `/private/tmp`.
- Fresh-chat prompt 5 rerun passed in thread
  `019f4613-1145-7783-bc34-73f9f7b276b4` using installed plugin
  `0.1.0+codex.20260709083506` and saved Longbridge standard OHLCV artifacts.
  The rerun reported QQQ/SOXX/DRAM prior context, 1D main timeframe with 1H
  auxiliary confirmation, support/resistance, add/trim/pause zones,
  proportional sizing, cost-context disclosure, weekly event mapping, and no
  broker/account reads or repo/runtime writes.

Runtime snapshot normalization repair follow-up:

- Added `repair_portfolio_snapshot.py`, which consumes an existing standard
  `portfolio_snapshot.csv` and remaps stale/unmapped `underlying`,
  `instrument_type`, and `theme_id` fields before position daily reports.
- Fixture verification covers stale/unmapped product/theme cases such as
  `QQQ.US` misclassified as common stock, `SOXX` missing sector ETF mapping,
  and `TSMX.US` missing leveraged ETF / AI hardware leverage mapping.
- The script writes to a separate output path by default and requires
  `--allow-same-path` for in-place repair. It performs No live broker reads and
  No order actions.
- Private 2026-07-09 header-only runtime smoke wrote a separate repaired copy
  under `/private/tmp` without overwriting runtime state.
- Longbridge Terminal CLI forward smoke then read the authorized account through
  the read-only `longbridge portfolio --format json` command, saved raw JSON
  only under `/private/tmp`, normalized it with `longbridge_cli_adapter.py`,
  repaired the standard snapshot with `repair_portfolio_snapshot.py`, and
  rendered `position_daily_report.py` from the repaired snapshot. No raw
  Longbridge holdings or account facts were copied into this repository.
- The Longbridge repair pass changed 6 of 8 standard snapshot rows, proving the
  repair path is useful on a non-empty private broker snapshot. Runtime
  overwrite remains user-confirmed; the forward smoke used separate temp files.

Live read-only broker forward-test follow-up:

- IBKR read-only positions/balances had already been forward-smoked through
  `ibkr_connector_adapter.py` and `position_daily_report.py`.
- Longbridge Terminal CLI read-only forward smoke passed on 2026-07-09:
  token/connectivity check succeeded, `longbridge portfolio --format json`
  produced a private JSON snapshot under `/private/tmp`,
  `longbridge_cli_adapter.py` wrote an 8-row standard `portfolio_snapshot.csv`,
  `repair_portfolio_snapshot.py` wrote a repaired standard snapshot, and
  `position_daily_report.py` rendered a private report from that repaired view.
- Current Codex MCP/tool discovery still does not expose a native Longbridge
  broker skill tool in this session. This does not block the local 1.0 broker
  runtime-view requirement because the v1 supported Longbridge Terminal CLI path
  is forward-tested; native skill forward testing remains a follow-up when the
  capability is visible.

## P0 Gaps Before Claiming Local 1.0 Complete

| Gap | Why it blocks the 1.0 complete claim | Next development action |
| --- | --- | --- |
| Prepared setup rows not yet populated in private runtime | The script path now exists and is verified, but today's `trade-plans.csv` and `intraday-watchlist.csv` remain header-only. Current Active Market Plan setup pool is watch-only / no new exposure until reopened. | Ask the user to confirm which setup rows should be tracked today, run `prepare_setup_rows.py` against the private runtime, then rerun prompt 3 in a fresh chat. |

## P1 Follow-ups

- Decide whether `ops-state.md` should be required for every fresh Daily Ops
  run or created on demand by the startup flow.
- Add a concise acceptance-results contract only after the pass/fail record
  becomes stable enough to validate mechanically.
- Forward-test the visual trigger policy with real `macro-panel.json`,
  `portfolio_snapshot.csv`, and a DRAM/SOXX/QQQ PA prompt.

## Promotion Rule

Do not promote `dev` to `master` or call this local workflow 1.0 complete until:

1. the remaining PARTIAL prompt either becomes PASS or has an explicit accepted
   non-goal decision;
2. the P0 gaps above are resolved or formally scoped out by the user;
3. the final `dev` branch passes `scripts/verify-plugin.sh`,
   `scripts/verify-mvp.sh`, the core contract suite, and `git diff --check`;
4. no private runtime or broker data has entered the public repository.
