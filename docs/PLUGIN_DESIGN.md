# Plugin Design (Superseded)

This document previously described the prompt-heavy 0.1.x architecture,
including mutable KVN/SQLite compatibility paths and broad prose contracts.
Those implementation details are not the current design.

The accepted 0.2.0 architecture is defined by:

- [`ADR 0009`](adr/0009-research-result-delivery-contract.md);
- the canonical [`SKILL.md`](../skills/trading-research-system/SKILL.md);
- the active [`ROADMAP.md`](ROADMAP.md);
- the minimal [`MVP_RUNBOOK.md`](MVP_RUNBOOK.md).

Historical decisions and 0.1.x delivery evidence remain in
`docs/PROJECT_LOG.md` and the archived acceptance records. Do not use this file
as an implementation contract.
