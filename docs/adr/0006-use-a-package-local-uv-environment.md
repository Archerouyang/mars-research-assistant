# 使用组合包内的 uv 虚拟环境

Mars Research Assistant 在组合包内维护 `.venv`，安装器落盘后必须执行
`uv sync --locked`，所有子 Skill 共享该隔离环境。安装不污染用户项目或全局 Python，
缺少 uv、依赖同步失败或环境验证失败都会使整个原子安装失败；安装器不会静默回退到 pip。
受管升级仅在 `uv.lock` 变化时重建环境。仓库自带安装器是唯一受支持的受管安装入口；手工
复制和第三方 Skill 下载器可以发现文件，但不承诺创建环境、执行自检或安全升级。
