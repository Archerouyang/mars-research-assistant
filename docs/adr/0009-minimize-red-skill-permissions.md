# 最小化 RED Skill 权限

RED Skill 根入口只披露并使用四类能力：研究所需的公开网络读取、受管安装器执行
`uv sync --locked` 与离线验证、受管安装目录和用户指定工件目录的本地写入，以及取得独立
明确确认后的 Google Drive 写入。组合包不访问券商账户、持仓、订单、交易执行、系统凭据、
FMP/API key，不运行后台任务、每日检测、隐式缓存或遥测。权限不足时应降级或失败并说明，
不能扩大权限面。
