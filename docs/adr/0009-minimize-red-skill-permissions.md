# 最小化 RED Skill 权限

RED Skill 根入口只披露并使用四类能力：研究所需的公开网络读取、技术面分析环境门（以及
复用它的完整包安装器）执行锁定 uv 同步与离线验证、受管安装目录和用户指定工件目录的
本地写入，以及取得独立明确确认后的 Google Drive 写入。其他五个 Skill 不触发 uv。
组合包不访问券商账户、持仓、订单、交易执行、系统凭据、FMP/API key，不运行后台任务、
每日检测、隐式缓存或遥测。权限不足时应降级或失败并说明，不能扩大权限面。
